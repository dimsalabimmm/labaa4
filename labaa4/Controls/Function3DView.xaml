<UserControl x:Class="labaa4.Controls.Function3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Background="Transparent">
    <Grid>
        <Viewport3D x:Name="SurfaceViewport"
                    ClipToBounds="True"
                    MouseRightButtonDown="Viewport3D_MouseRightButtonDown"
                    MouseRightButtonUp="Viewport3D_MouseRightButtonUp"
                    MouseMove="Viewport3D_MouseMove">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="MainCamera"
                                   Position="0,2.2,5.5"
                                   LookDirection="0,-0.9,-5.5"
                                   UpDirection="0,1,0"
                                   FieldOfView="55" />
            </Viewport3D.Camera>
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <Model3DGroup>
                        <AmbientLight Color="#44202020" />
                        <DirectionalLight Color="#FFF5F5F5" Direction="-1,-1,-2" />
                        <DirectionalLight Color="#44FF9800" Direction="1,-0.2,-1" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>
            <ModelVisual3D x:Name="SurfaceModel" />
        </Viewport3D>
    </Grid>
</UserControl>

